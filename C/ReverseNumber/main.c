#include <stdio.h>
#include <stdlib.h>

int reverse_num(int num)
{
    char reversed_num_string[];
    do{
        reversed_num_string + = num % 10
    }
    while(num != 0)
    return atoi(reversed_num_string);
}
int main(){

    printf("Hello world");
    return 0;

}
